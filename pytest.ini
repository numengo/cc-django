[pytest]
addopts = -x --tb=short
python_paths = .
norecursedirs = .tox .git */migrations/* */static/* docs venv */{{cookiecutter.app_name}}/*
markers =
    flake8: Run flake8 on all possible template combinations
    black: Run black on all possible template combinations
